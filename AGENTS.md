# 开发入口

本项目由独立开发者维护，采用小步增量迭代。默认单 agent 串行完成，不设置团队角色或固定冲刺仪式。

## 每次接手

修改前只读以下短文档，再按任务定位源码：

1. [CONTEXT.md](CONTEXT.md)：稳定术语与约束。
2. [NEXT_AGENT.md](NEXT_AGENT.md)：当前状态和接手动作。
3. [工作方式](docs/agent-collaboration.md)：增量交付规则。

任务优先级只在 [backlog](docs/backlog.md) 维护；构建、验证与发布操作只在 [开发手册](docs/development.md) 维护。不要默认扫描整套文档、历史诊断或整个仓库。

## 修改规则

- 保留并补充必要中文注释：核心模块、类、关键方法说明职责；复杂逻辑说明原因。保留有效注释，避免逐行解释明显代码。
- 不覆盖用户修改，不运行 git reset --hard 或 git clean；不擅自提交、推送或发布。
- 不读取、输出或提交真实账号密码、Cookie、数据库、日志或证书；测试使用虚构数据。
- 按改动范围验证；文档修改检查链接和一致性，无需重建产品。
- 完成后更新原有事实和任务状态，不追加重复交接历史；未验证项明确保留。

仅在用户要求并行时拆分独立子任务，先约定文件范围；公共文件由当前主任务修改。子任务只报告变更、验证和剩余风险。


