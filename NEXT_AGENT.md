# 当前交接

更新：2026-09-23。只保留当前事实；任务状态唯一来源为 [backlog](docs/backlog.md)。

## 基线

- 用户已冻结首版 v0.1.0；现阶段维护现有功能，不主动扩展产品范围。
- 已实现多开、账号编辑/明文凭据、单次自动登录、刷新、静音、变速、比例适配和1秒黑底；图标已更换。
- 发布包已压缩为4文件，自带.NET；路径、大小、哈希和发行证据见 [首版记录](docs/release-v0.1.0.md)。
- 历史自动测试60/60通过；精简包图标、面板、SQLite读取通过。精简前 .7 自动登录和游戏启动菜单、全屏、最大化还原、刷新有实机证据。
- **精简后的正式包游戏启动仍待用户反馈**，不得把 .7 验收替代正式包验收。
- 旧进程和构建缓存已清理约2.74 GiB；只保留当前发行产物，旧预览包链接已失效。源码、测试和诊断源码保留。
- 首版源码已于本轮创建首个提交（120文件），尚未推送；许可证未选，远程已配置，CI工作流存在但未验证托管运行。
- 本轮修复 UPD-01：游戏入口改为每次启动解析平台当前版本，不再固定 v3680d；xUnit 61/61通过，实机探针解析到 v3690g。原生宿主本次未编译，见 backlog 的待验证项。

## 接手动作

先处理用户本轮明确任务；无新任务时沿 backlog 的“当前”项收集验收结果，不擅自启动“以后”候选。缺失用户结果时可检查构建/诊断材料，但不要重复索要已回答信息。

本轮仅重组文档，未改产品代码、未重建或改写已交付 v0.1.0 ZIP。发行包中的 README 是构建时快照，后续构建将使用新的简版。

## 容易回归的地方

- 游戏入口每次启动解析平台当前版本；不要把某一版包装页写回常量，否则游戏更新后旧地址下线。解析失败按缓存、兜底地址回退，不抛异常。
- 管道读取用 Win32 ReadFile；不可恢复阻塞 fgets + fflush(NULL)，曾导致登录冻结。
- 登录表单 action 可为根相对路径；用户名提示文字是 value 伪占位符。
- 显示回执检查子窗口 WS_VISIBLE；父 HwndHost 加载时隐藏，不能用 IsWindowVisible 判断成功。
- 构建/测试/发布串行执行，避免共享 bin/obj 冲突。操作命令见 [开发手册](docs/development.md)。

