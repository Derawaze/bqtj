# 稳定上下文

此文件只维护术语和产品约束，不记录进度。当前状态见 [NEXT_AGENT](NEXT_AGENT.md)。

| 术语 | 含义 |
|---|---|
| 账号记录 | 本地启动记录，以稳定 Guid 为主键；名称可改，不等于平台认证身份 |
| 平台凭据 | 用户主动保存的4399账号、密码，与账号 Guid 绑定，本地 SQLite 明文保存 |
| 登录态 | 平台 Cookie；当前仅在原生宿主进程内保留，刷新保留，退出后结束 |
| 游戏会话 | 一个账号的一次运行：独立 WPF 容器与其原生 Flash 子进程 |
| 多开 | 多个账号记录并行运行，不保证同一个平台账号多次登录不互踢 |
| 运行偏好 | 窗口档位、静音全账号共享；上一档变速按账号保存 |

## 已确定的边界

- Windows / .NET 10 / WPF / win-x86；使用本机32位 Flash ActiveX，不分发 Flash 或游戏资源。
- 自动打开账号密码页、填充并点击登录一次；保留平台校验，验证码或错误交由用户处理。
- 仅做运行时 Cookie 隔离，保留已有磁盘 Cookie；不承诺 Flash 本地共享对象完整隔离。
- 当前不推进持久登录态隔离，不新增 Windows 用户，不安装 Sandboxie；已否决的 AppContainer 方案不接回生产链。
- 全屏/最大化等比例居中并留黑边；加载就绪后固定1秒黑底。是否完全消除蓝闪仍未确认。
- 本地变速只作用于当前原生宿主中的 Flash 计时，不影响系统或服务端时间。
- 本地账号数据位于 %LOCALAPPDATA%/BqtjLauncher，清理构建产物不得触碰。

实现定位见 [架构](docs/architecture.md)，技术理由按需查 [文档索引](docs/README.md)。


