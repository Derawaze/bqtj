# 增量任务清单

更新：2026-09-23。此处是唯一开发计划；按实际反馈排序，不设未经确认的版本日期。

## 当前：验证首版精简包

**V1-01 · 待用户反馈**

- 结果：确认4文件 v0.1.0 包保留已验收的启动与多开能力。
- 范围：只验证现有包，不增加功能。已通过面板/图标/账号读取，自动测试和包校验见 [发行记录](release-v0.1.0.md)。
- 验收：启动已保存账号可自动进入游戏；第二账号可并行且不串号；刷新、关闭重开可用。优先收取已发出的用户反馈，失败时再建立最小复现。
- 完成后：记录对应包的结果；若有缺陷，下一增量只修影响使用的一个问题。

## 接下来：按反馈选一项

| ID | 触发条件与结果 | 最小验收 |
|---|---|---|
| ST-01 | 用户仍遇到蓝闪时，定位1秒黑底未覆盖的阶段 | 固定复现步骤和前后证据；无法稳定消除可记录保留，不盲目增加延迟 |
| ST-02 | 需要覆盖更多屏幕环境时，验证DPI与输入坐标 | 同一账号跨屏、最大化/全屏/还原及刷新后画面与点击一致 |
| REL-01 | 用户决定公开仓库/发布时，完成首次发布 | 先确定许可证与公开范围，再执行获授权的提交/推送、托管CI与发布检查 |

## 以后：候选，不自动开工

- 根据实际故障选择长时多开、Flash本地状态、音频设备切换或进程退出验证。
- 有真实恢复需求后再考虑账号备份、诊断导出。
- 安装器、签名、更新、扩展和替代运行时只保留方向，不提前设计。
- 持久登录态隔离仍暂缓；不重启已否决方案，除非用户重新提出需求。

## 已交付摘要

v0.1.0 已冻结：账号管理/凭据、单次自动登录、并行会话、窗口/静音/变速、图标和压缩发布。多开和 .4 自动填充获用户验收；.7 全屏/最大化还原/刷新有实机记录。全屏和最大化适配已实现，加载蓝闪保留观察项，不再重复列为“未开发”。详情按需读 [首版记录](release-v0.1.0.md) 与 [显示诊断](diagnostics/viewport-loading.md)。


